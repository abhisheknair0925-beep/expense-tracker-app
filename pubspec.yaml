name: expence_tracker_app
description: "Personal Finance App — Phase 1: Expense Tracker"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  provider: ^6.1.2
  sqflite: ^2.4.2
  path: ^1.9.1
  fl_chart: ^0.70.2
  google_fonts: ^6.2.1
  intl: ^0.20.2
  flutter_local_notifications: ^18.0.1
  timezone: ^0.10.1
  image_picker: ^1.1.2
  path_provider: ^2.1.5
  csv: ^6.0.0
  share_plus: ^10.1.4
  local_auth: ^2.3.0
  shared_preferences: ^2.3.0
  firebase_core: ^3.12.1
  firebase_auth: ^5.5.1
  cloud_firestore: ^5.6.5
  google_sign_in: ^6.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
